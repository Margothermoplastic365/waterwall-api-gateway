databaseChangeLog:
  - changeSet:
      id: gw-025-create-regions
      author: gateway-team
      changes:
        - createTable:
            schemaName: gateway
            tableName: regions
            columns:
              - column: { name: id, type: uuid, defaultValueComputed: gen_random_uuid(), constraints: { primaryKey: true } }
              - column: { name: name, type: varchar(255), constraints: { nullable: false } }
              - column: { name: slug, type: varchar(255), constraints: { nullable: false, unique: true } }
              - column: { name: endpoint_url, type: varchar(1024), constraints: { nullable: false } }
              - column: { name: data_residency_zone, type: varchar(100) }
              - column: { name: status, type: varchar(50), defaultValue: "ACTIVE" }
              - column: { name: created_at, type: timestamptz, defaultValueComputed: now() }
